---
phase: 06-runner-impl
type: execute
---

<objective>
Bootstrap the Rust runner CLI and establish repo layout for a neutral differential harness.

Purpose: Start execution with a minimal, independent driver that can wrap multiple implementations.
Output: Repo layout established and Rust runner scaffolded.
</objective>

<execution_context>
~/.codex/skills/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/ARCHITECTURE.md
@.planning/REPO_LAYOUT.md
@.planning/OUTPUT_SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runner workspace layout</name>
  <files>runner/Cargo.toml, runner/src/main.rs, schemas/output_schema.md, schemas/corpus_spec.md, corpus/README.md</files>
  <action>Create a `runner/` Rust binary crate with a minimal CLI scaffold and add top-level folders per `REPO_LAYOUT.md`. Copy the output schema and corpus spec into `schemas/` for co-location.</action>
  <verify>`runner` builds, and `schemas/` and `corpus/` directories exist with placeholder docs.</verify>
  <done>Repo layout exists and runner builds.</done>
</task>

<task type="auto">
  <name>Task 2: Define core types in runner</name>
  <files>runner/src/lib.rs, runner/src/main.rs</files>
  <action>Define `Outcome`, `Target`, and `RunRecord` types consistent with `.planning/OUTPUT_SCHEMA.md`. Implement a stub runner that accepts an input file and prints a JSON record with placeholder results.</action>
  <verify>Running the CLI emits a JSON stub matching the schema fields.</verify>
  <done>Core types and stub runner are implemented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `runner` builds successfully
- [ ] CLI emits JSON with required fields
</verification>

<success_criteria>

- Neutral runner scaffold exists and aligns to output schema
- Directory layout matches `REPO_LAYOUT.md`
  </success_criteria>

<output>
After completion, create `.planning/phases/06-runner-impl/06-01-SUMMARY.md`:

# Phase 6 Plan 01: Runner Bootstrap Summary

**[One-liner describing runner scaffold created]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `runner/Cargo.toml` - Rust runner crate
- `runner/src/main.rs` - CLI scaffold
- `schemas/output_schema.md` - Output schema copy
- `schemas/corpus_spec.md` - Corpus spec copy
- `corpus/README.md` - Corpus placeholder

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 06-02-PLAN.md
</output>
