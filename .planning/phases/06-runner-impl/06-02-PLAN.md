---
phase: 06-runner-impl
type: execute
---

<objective>
Implement target adapters for Go and Rust implementations and emit real output records.

Purpose: Enable differential runs against `go-dasl` and `atproto-dasl`.
Output: Adapter wrappers and runner integration.
</objective>

<execution_context>
~/.codex/skills/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/ARCHITECTURE.md
@.planning/OUTPUT_SCHEMA.md
@.planning/CORPUS_SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Go adapter wrapper</name>
  <files>targets/go_dasl/README.md, targets/go_dasl/run.sh</files>
  <action>Create a wrapper script or binary that invokes `go-dasl` decoding and returns normalized output (accept/reject, error class, canonical bytes). Keep it CLI-driven and deterministic.</action>
  <verify>Wrapper prints JSON output for a sample input.</verify>
  <done>Go adapter wrapper works on a sample input.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Rust adapter wrapper</name>
  <files>targets/rust_atproto_dasl/README.md, targets/rust_atproto_dasl/run.sh</files>
  <action>Create a wrapper that invokes `atproto-dasl` via a small Rust or CLI shim and returns normalized output.</action>
  <verify>Wrapper prints JSON output for a sample input.</verify>
  <done>Rust adapter wrapper works on a sample input.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate adapters in runner</name>
  <files>runner/src/main.rs, runner/src/lib.rs</files>
  <action>Call both adapters for each input and emit full output records with invariant status.</action>
  <verify>Runner emits records for both targets on a sample corpus entry.</verify>
  <done>Runner produces multi-impl output with invariant status.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Adapter wrappers emit JSON with required fields
- [ ] Runner produces full output record for a sample input
</verification>

<success_criteria>

- Go and Rust adapters integrated into runner
- Output records align with schema and include invariant status
  </success_criteria>

<output>
After completion, create `.planning/phases/06-runner-impl/06-02-SUMMARY.md`:

# Phase 6 Plan 02: Adapter Integration Summary

**[One-liner describing adapters integrated and outputs emitted]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `targets/go_dasl/*` - Go adapter wrapper
- `targets/rust_atproto_dasl/*` - Rust adapter wrapper
- `runner/src/*` - Runner integration

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase complete, ready for next phase
</output>
