# atproto-dasl Adapter

This wrapper invokes the `atproto-dasl` implementation via a small Rust adapter and emits JSON per `targets/target-spec.md`.

Builds the adapter on first run. Canonical bytes are returned as hex.
