# go-dasl Adapter

This wrapper invokes the `go-dasl` implementation via a tiny Go adapter and emits JSON per `targets/target-spec.md`.

Builds the adapter on first run. Canonical bytes are returned as hex.
