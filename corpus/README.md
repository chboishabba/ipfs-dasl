# Corpus

This directory holds:

- `seed/` initial seeds
- `failures/` minimized divergence or crash repros
- `crashes/` crash repros if separated

Store only minimized failing vectors and a small seed set to keep size bounded.
